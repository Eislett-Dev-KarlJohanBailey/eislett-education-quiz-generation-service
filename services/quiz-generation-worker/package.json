{
  "name": "@services/quiz-generation-worker",
  "version": "1.0.0",
  "description": "SQS consumer: generate quiz via OpenAI and save to DynamoDB",
  "main": "dist/handler/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "package": "npm run build && esbuild src/handler/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js && cd dist && zip -r ../function.zip index.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.975.0",
    "@aws-sdk/client-secrets-manager": "^3.975.0",
    "@aws-sdk/client-sqs": "^3.995.0",
    "@aws-sdk/lib-dynamodb": "^3.975.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/node": "^25.0.10",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
